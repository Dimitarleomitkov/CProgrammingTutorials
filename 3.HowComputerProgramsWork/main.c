// These are header files
#include <stdio.h>
// They include already written code (functions) from "libraries"
#include <stdlib.h>

// This is the first function of every program
int main()
{
    /* Inside the "{" and "}" is the scope of the function
       The code is implemented here. */
    // Call to the function printf ()
    printf("Hello world!\n");
    // This returns a succession code for the program to know whether it failed
    return 0;
}
